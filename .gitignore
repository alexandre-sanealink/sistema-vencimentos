# Arquivos de dependências (nunca devem ir para o GitHub)
/node_modules
/backend/node_modules
/frontend/node_modules

# Arquivos de ambiente (contêm senhas e segredos)
.env
backend/.env
frontend/.env

# Logs de erro
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Pasta de build de produção (se você usar no futuro)
/dist
/build